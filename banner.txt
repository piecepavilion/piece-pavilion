# ============================================================
#  PIECE PAVILION — SITE ANNOUNCEMENT BANNER (FALLBACK ONLY)
#  The banner is normally edited in the "PiecePavilion Site Banner"
#  Google Sheet (tile on /employees). This file is only used if the
#  sheet can't be reached. Edits here go live ~1 min after commit.
#
#  enabled    YES shows the banner on every page, NO hides it
#  message    the bold headline (emoji welcome — paste them in)
#  link_text  the smaller call-to-action after the headline
#  link       where clicking goes: a site page (/blog/...), a full https:// URL,
#             or the word LATEST to auto-point at the newest blog post.
#  Lines starting with # are ignored.
# ============================================================
enabled=YES
message=🎵 New on the Blog: Olivia Rodrigo Gets Her Own LEGO
link_text=Read the latest blog post here →
link=LATEST
